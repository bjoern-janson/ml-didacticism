# AG/1 External Transport Milestone — T1 through T5

**Status:** FROZEN TRANSPORT MILESTONE  
**Architecture under test:** `AG/1 = {RELATION, REPRESENTATION} + {SOURCE_PROVENANCE, OPEN}`  
**Purpose:** preserve the evidential state after five heterogeneous external tests before Transport 06 introduces observability/interface-modification pressure

---

# 1. Frozen transport ledger

```text
Transport 01 — software incident / debugging
GitLab January 31, 2017 database incident
→ PASS

Transport 02 — experimental science / metrology
OPERA / ICARUS neutrino-velocity anomaly
→ PASS

Transport 03 — same presented evidence / competing analyses
Challenger launch-decision record
→ PASS

Transport 04 — same observation / different underlying worlds
CDC tuberculosis-testing non-identifiability
→ PASS

Transport 05 — representation → intervention → changed world → revised representation
Mars Climate Orbiter navigation failure
→ PASS
```

The strongest calibrated claim currently earned is:

```math
\boxed{
\textbf{AG/1 has survived five heterogeneous external reconstruction tests without architectural enlargement.}
}
```

No external corpus tested through T5 has earned a new primitive.

---

# 2. What T1–T5 have jointly pressured

T1:

```math
\boxed{\text{historical target}\neq\text{operator represented target}}
```

T2:

```math
\boxed{\text{experimental operation}\neq\text{measurement result}\neq\text{physical interpretation}}
```

T3:

```math
\boxed{D_{shared}\rightarrow\rho_A,\quad D_{shared}\rightarrow\rho_B,\quad \rho_A\neq\rho_B}
```

T4:

```math
\boxed{O(H_1)=O(H_2)\quad\text{while}\quad H_1\neq H_2}
```

T5:

```math
\boxed{\rho_t\rightarrow a_t\rightarrow\mathcal H_{t+1}\rightarrow o_{t+1}\rightarrow\rho_{t+1}}
```

with the additional negative result:

```math
\boxed{\text{feedback}\not\Rightarrow\text{correct revision}}
```

Together these show that frozen AG/1 has so far reconstructed passive representation, competing representation, observational non-identifiability, and representation-driven causal intervention without adding new architecture species.

---

# 3. Claim ceiling

This milestone does **not** establish:

```text
AG/1 is universal.
AG/1 is mathematically minimal across all formalisms.
AG/1 has a known transportability envelope.
Five passes estimate the probability of future transport success.
Every adaptive or epistemic system reduces to AG/1.
Feedback guarantees learning.
```

The evidence supports only the five bounded transport results above.

---

# 4. Next orthogonal pressure — T6

Transport 06 changes the failure mode again.

T1–T5 leave the observation/measurement interface structurally available during the loop.

T6 asks whether AG/1 can preserve an intervention that changes the conditions under which later evidence can be obtained:

```math
\boxed{
\rho_t
\rightarrow
a_t
\rightarrow
\mathcal H_{t+1}+\mathcal I_{t+1}
\rightarrow
o_{t+1}
\rightarrow
\rho_{t+1}
}
```

where `\mathcal I` is **not** an architecture primitive. It is shorthand for the concrete relation topology connecting world processes, sensors/measurement paths, alerts, recipients, and available observations.

The intended pressure is:

```math
\boxed{\text{world changed}\neq\text{observation path changed}\neq\text{information became available}}
```

and especially:

```math
\boxed{\text{capability}\neq\text{future error detectability}}
```

T6 must attempt to reconstruct this using only source-earned relations plus representation scopes.

Forbidden architecture additions include:

```text
INTERFACE
OBSERVABILITY
CORRIGIBILITY
CHANNEL
SENSOR_STATE
MONITORING_STATE
```

as new primitives.

---

# 5. Causal boundary remains frozen

Transport 06 may only return:

```text
PASS
FAIL_RECONSTRUCTION
FAIL_HIDDEN_PARAMETER
OPEN_TEST
```

It may not modify AG/1.

A failure must be recorded as:

```text
smallest failing witness
→ missing distinction
→ attempted AG/1 reconstruction
→ exact reconstruction failure
```

Only after such a failure may a separate successor architecture be proposed.

AG/1 and this T1–T5 milestone remain frozen.