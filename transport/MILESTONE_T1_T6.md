# AG/1 External Transport Milestone — T1 through T6

**Status:** FROZEN TRANSPORT MILESTONE  
**Architecture under test:** `AG/1 = {RELATION, REPRESENTATION} + {SOURCE_PROVENANCE, OPEN}`  
**Purpose:** preserve the evidential state after six heterogeneous external tests before Transport 07 introduces deliberate challenge-selection pressure

---

# 1. Frozen transport ledger

```text
Transport 01 — software incident / debugging
GitLab January 31, 2017 database incident
→ PASS

Transport 02 — experimental science / metrology
OPERA / ICARUS neutrino-velocity anomaly
→ PASS

Transport 03 — same presented evidence / competing analyses
Challenger launch-decision record
→ PASS

Transport 04 — same observation / different underlying worlds
CDC tuberculosis-testing non-identifiability
→ PASS

Transport 05 — representation → intervention → changed world → revised representation
Mars Climate Orbiter navigation failure
→ PASS

Transport 06 — action modifies future observation topology
Deepwater Horizon / Macondo observability-interface modification
→ PASS
```

The strongest calibrated claim currently earned is:

```math
\boxed{
\textbf{AG/1 has survived six heterogeneous external reconstruction tests without architectural enlargement.}
}
```

No external corpus tested through T6 has earned a new primitive.

---

# 2. What T1–T6 have jointly pressured

T1:

```math
\boxed{\text{historical target}\neq\text{operator represented target}}
```

T2:

```math
\boxed{\text{experimental operation}\neq\text{measurement result}\neq\text{physical interpretation}}
```

T3:

```math
\boxed{D_{shared}\rightarrow\rho_A,\quad D_{shared}\rightarrow\rho_B,\quad \rho_A\neq\rho_B}
```

T4:

```math
\boxed{O(H_1)=O(H_2)\quad\text{while}\quad H_1\neq H_2}
```

T5:

```math
\boxed{\rho_t\rightarrow a_t\rightarrow\mathcal H_{t+1}\rightarrow o_{t+1}\rightarrow\rho_{t+1}}
```

with:

```math
\boxed{\text{feedback}\not\Rightarrow\text{correct revision}}
```

T6:

```math
\boxed{
\rho_t
\rightarrow a_t
\rightarrow \Delta InterfaceView
\rightarrow \Delta o_{future}
\rightarrow \rho_{future}
}
```

where `InterfaceView` is a derived query over concrete process/measurement/signal/recipient relations rather than a primitive carrier.

T6 additionally established:

```math
\boxed{\text{observability}\neq\text{corrigibility}}
```

because increased or degraded evidence paths did not guarantee corresponding correction behavior.

---

# 3. Claim ceiling

This milestone does **not** establish:

```text
AG/1 is universal.
AG/1 is mathematically minimal across all formalisms.
AG/1 has a known transportability envelope.
Six passes estimate the probability of future transport success.
Every interface is reducible in every domain.
More observability guarantees better revision.
All active information seeking is already derived.
```

The evidence supports only the six bounded transport results above.

---

# 4. Next orthogonal pressure — T7

Transport 07 changes the failure mode again.

T6 showed that actions can alter future contradiction paths.

T7 asks whether an agent can **select among candidate observation-changing actions because of represented expected discriminatory value** without adding a new primitive.

Target structure:

```math
\boxed{
\rho_t
\rightarrow
\{a_1,a_2,\ldots,a_n\}
\rightarrow
SELECT(a_k\mid\rho_t)
\rightarrow
o_{t+1}
\rightarrow
\rho_{t+1}
}
```

with the prospective relation:

```math
\boxed{
\text{action }a_k\text{ is selected because represented consequences distinguish live alternatives better than another available action.}
}
```

The exact distinction under pressure is:

```math
\boxed{
\text{uncertainty}
\neq
\text{candidate action}
\neq
\text{represented expected discrimination}
\neq
\text{selection}
\neq
\text{execution}
\neq
\text{realized evidence}
}
```

Forbidden architecture additions include:

```text
QUERY
EXPERIMENT_DESIGN
ACTIVE_LEARNING
INFORMATION_GAIN
VALUE_OF_INFORMATION
EXPLORATION
CHALLENGE_SELECTION
```

as new primitives.

Source-earned typed relations with those local meanings remain allowed.

---

# 5. Causal boundary remains frozen

Transport 07 may only return:

```text
PASS
FAIL_RECONSTRUCTION
FAIL_HIDDEN_PARAMETER
OPEN_TEST
```

It may not modify AG/1.

A failure must be recorded as:

```text
smallest failing witness
→ missing distinction
→ attempted AG/1 reconstruction
→ exact reconstruction failure
```

Only after such a failure may a separate successor architecture be proposed.

AG/1 and this T1–T6 milestone remain frozen.
